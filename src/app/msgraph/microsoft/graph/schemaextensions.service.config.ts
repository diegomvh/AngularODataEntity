import { EntitySetConfig } from 'angular-odata';

//#region ODataApi Imports
//#endregion

export const SchemaExtensionsServiceConfig = {
  name: "schemaExtensions",
  annotations: []
} as EntitySetConfig;
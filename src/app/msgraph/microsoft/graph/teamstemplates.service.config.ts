import { EntitySetConfig } from 'angular-odata';

//#region ODataApi Imports
//#endregion

export const TeamsTemplatesServiceConfig = {
  name: "teamsTemplates",
  annotations: []
} as EntitySetConfig;
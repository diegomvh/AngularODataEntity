import { EntitySetConfig } from 'angular-odata';

//#region ODataApi Imports
//#endregion

export const DirectoryRolesServiceConfig = {
  name: "directoryRoles",
  annotations: []
} as EntitySetConfig;
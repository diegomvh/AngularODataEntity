import { ServiceConfig } from 'angular-odata';

//#region ODataApi Imports
//#endregion

export const RecursosUsuariosServiceConfig = {
  name: "recursosUsuarios",
  annotations: []
} as ServiceConfig;